# Minimal release workflow - diagnostic test
name: Release

on:
  workflow_dispatch:

jobs:
  test:
    name: Diagnostic
    runs-on: ubuntu-latest
    steps:
      - name: Echo
        run: echo "Release workflow works"
