# Third-party dependencies build file
# Currently npm-managed, Bazel for queryability

package(default_visibility = ["//visibility:private"])

# Export package.json files for dependency tracking
exports_files([
    "README.md",
])

# Placeholder for future rules_js integration
# When migrating to hermetic builds, add:
#
# load("@npm//:defs.bzl", "npm_link_all_packages")
# npm_link_all_packages(name = "node_modules")

filegroup(
    name = "licenses",
    srcs = glob(["licenses/**"]),
    visibility = ["//visibility:public"],
)
